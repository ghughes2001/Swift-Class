import UIKit

@UIApplicationMain
class AppDelegate: UIResponder, UIApplicationDelegate {
    var window: UIWindow?

    func application(_ application: UIApplication, didFinishLaunchingWithOptions launchOptions: [UIApplication.LaunchOptionsKey: Any]?) -> Bool {
        /**
         // TODO: - Set a breakpoint on the following `print()` statement to illustrate when this function is called
         -
         // HINT: - `cmd + R` and wait
         -
         */
        print(#function)
        return true
    }

    func applicationWillResignActive(_ application: UIApplication) {
        /**
         // TODO: - Set a breakpoint on the following `print()` statement to illustrate when this function is called
         -
         // HINT: - `shift + cmd + H`
         -
         */
        print(#function)
    }

    func applicationDidEnterBackground(_ application: UIApplication) {
        /**
         // TODO: - Set a breakpoint on the following `print()` statement to illustrate when this function is called
         -
         // HINT: - `shift + cmd + H`
         -
         */
        print(#function)
    }

    func applicationWillEnterForeground(_ application: UIApplication) {
        /**
         // TODO: - Set a breakpoint on the following `print()` statement to illustrate when this function is called
         -
         // HINT: - Background the App and then click on the icon again
         -
         */
        print(#function)
    }

    func applicationDidBecomeActive(_ application: UIApplication) {
        /**
         // TODO: - Set a breakpoint on the following `print()` statement to illustrate when this function is called
         -
         // HINT: - `cmd + R` and wait
         -
         */
        print(#function)
    }

    func applicationWillTerminate(_ application: UIApplication) {
        /**
         // TODO: - Set a breakpoint on the following `print()` statement to illustrate when this function is called
         -
         // HINT: - `shift + cmd + H + H` and slide the app window upwards, out of view
         -
         */
        print(#function)
    }
}
